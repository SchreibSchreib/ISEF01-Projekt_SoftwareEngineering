spring.application.name=backend
# --------------------------------------------
# 🗄️ Datenbankverbindung
# --------------------------------------------
spring.datasource.url=spring.datasource.url=jdbc:mariadb://localhost:3306/quizapp
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}


# Der Treiber für MySQL
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# --------------------------------------------
# 🧱 JPA / Hibernate Einstellungen
# --------------------------------------------
# "update" erstellt Tabellen automatisch anhand der Entities,
# ohne bestehende Daten zu löschen
spring.jpa.hibernate.ddl-auto=update

# Zeigt SQL-Statements in der Konsole (zum Debuggen)
spring.jpa.show-sql=true

# Optional: Dialekt für MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# --------------------------------------------
# 🌐 Server Konfiguration
# --------------------------------------------
# Standardport (optional, 8080 ist Default)
server.port=8080
